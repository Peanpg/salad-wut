# แก้ปัญหาอัปโหลดรูป Google Drive

เวอร์ชันนี้แสดง Error จริงจาก Apps Script ไม่เปลี่ยนทุก Error เป็นข้อความเรื่องสิทธิ์ Drive อีกต่อไป และลดขนาดรูปก่อนส่งเป็นด้านยาวไม่เกิน 1200 px คุณภาพ JPEG 72%

## หลังอัปเดต Code.gs

1. บันทึก Code.gs
2. เลือก `authorizeSetup` แล้วกดเรียกใช้
3. เลือก `testUploadPermission` แล้วกดเรียกใช้ ต้องขึ้นดำเนินการเสร็จแล้ว
4. Deploy > จัดการการทำให้ใช้งานได้ > ดินสอ > เวอร์ชันใหม่
5. ต้องตั้ง `ดำเนินการในฐานะ: ฉัน` และ `ผู้ที่มีสิทธิ์เข้าถึง: ทุกคน`
6. เปิด `<WEB_APP_URL>?test=drive` ต้องเห็น `"driveAuthorized":true`

ถ้า `?test=drive` Error แปลว่า Deployment ของ Web App ยังไม่ได้ทำงานในฐานะเจ้าของ แม้ `authorizeSetup` ใน Editor จะผ่านก็ตาม
